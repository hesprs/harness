# The Ideal Harness

`harness.config.ts` + multi-agent collaboration based on Pi.

## Usage

```sh
bun install
bun compile

ln -s ../../harness/dist ./packages/configuration/agent/extensions
ln -s "$PWD/packages/configuration" ~/.pi
```
